# Backend API base URL
# Leave empty for local dev (Vite proxy handles /api → localhost:5000)
# Set this to your HuggingFace Space URL for production:
# Example: https://YOUR-USERNAME-ytmusic-backend.hf.space
VITE_API_BASE=
